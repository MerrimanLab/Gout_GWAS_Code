This directory contains all the scripts involved in the per-cohort, and
across-cohort QC.

`process_sumstats/` directory contains all the scripts used to pre-clean all the summary stats
before they can be QCed.

`main_qc/` directory contains the main QC pipeline scripts that uses the
pre-cleaned summary stats from the `process_sumstats/` directory.

